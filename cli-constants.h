#pragma once
#pragma once
#ifndef CLI_CONSTANTS_H
#define CLI_CONSTANTS_H

#include <string>
#include <map>

const std::string CLI_CONTINUE = (std::string)"continue";
const std::string CLI_EXIT = (std::string)"exit";

#endif